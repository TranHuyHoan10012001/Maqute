name: Build
on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  sonarcloud:
    name: SonarCloud
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Install dependencies
        run: yarn
      - name: Test and coverage
        run: yarn jest --coverage --coverageReporters="json-summary"
      #   - name: SonarCloud Scan
      #     uses: SonarSource/sonarcloud-github-action@master
      #     env:
      #       SONAR_TOKEN: ${{ secrets. SONAR_TOKEN }}
      - name: 6. Update code coverage badge
        run: |
          npx update-badge --set 70

      #   - name: Set code coverage commit status 📫
      #     run: npx -p check-code-coverage set-gh-status
      #     env:
      #       GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Autoupdate
        uses: mikeal/publish-to-github-action@feature/test-coverage
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
